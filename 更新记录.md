# CPU 访存 Trace 流水线分析器 · 更新记录

> 命名规则：v0.x 为功能迭代，v0.x.y 为热修复。修订日期为功能上线日期。

---

## v0.06.9 · 2025-08-07（SVG 真矢量 + 悬浮提示改自定义气泡消除整窗闪烁）

> 本轮修复 v0.06.8 遗留的两个缺陷：**SVG「整个画布 / 有效区域」实为位图包裹**（非矢量）**，以及交互对象悬浮提示触发**操作系统原生 tooltip 导致整个浏览器闪烁**。

### 1. SVG 导出改为真矢量（缺陷修复）
v0.06.8 的「整个画布 / 有效区域」把画布位图 `toDataURL` 塞进 `<image>` 标签，放大即糊、不算矢量。本轮新增录制型 Canvas2D 上下文 `makeSvgCtx()`：

- 临时把各视图的 `ctx` 替换为录制器，重跑既有绘制函数（`tlDraw` / `accRender` / `pvDraw` / `miniDraw`）；
- 把 `fillRect` / `strokeRect` / 路径 `fill` / `stroke` / `fillText` 等调用录制成真正的 `<rect>` / `<path>` / `<text>` SVG 图元；
- 导出产物为纯矢量，可任意缩放不失真，三种范围（整页 / 整个画布 / 有效区域）均经 `DOMParser` 校验 **well-formed** 且 **0 个 `<image>` 标签**。

| 范围 | 产物特征（v0.06.9） |
|------|------|
| 整页 | 克隆 `#app` → `<foreignObject>` 包矢量 `<svg>`（画布内容替换为录制得到的矢量 `<svg>`） |
| 整个画布 | 录制当前激活画布为矢量 `<svg>`（`<path>/<rect>/<text>`，无位图） |
| 有效区域 | 录制后按内容包围盒设置 `viewBox` 裁剪，仍为矢量 |

### 2. 修复「Attribute viewBox redefined」导致文件损坏
v0.06.8 的 `exportCanvasSvg` 在录制出的 `<svg>` 上**再用正则追加第二个 `viewBox`**（第一个已由 `toSvg` 生成），导致 XML 校验报 `Attribute viewBox redefined`、文件无法打开。改为把裁剪矩形**直接传入** `recordCanvasSVG → toSvg`，由 `toSvg(w,h,vx,vy)` 一次性正确生成 `viewBox` / `width` / `height`，杜绝重复属性。

### 3. 悬浮提示改自定义气泡，消除整窗闪烁（缺陷修复）
v0.06.8 的 `bindTooltips()` + `lazyTip()` 给交互元素挂**原生 `title`**，会触发操作系统原生 tooltip——鼠标移到按钮上即**连带整个浏览器闪一下**。本轮：

- 移除 `bindTooltips()` / `lazyTip()` 及所有内联 `title=`；
- 新增 `bindTips()`：启动时**清空全部原生 `title`**（并兜底：悬浮到残留原生 `title` 的动态元素时立即清除），统一用事件委托（mouseover / mousemove / mouseout）在 `#app` 上驱动**自定义 `#tip` 气泡**；
- 覆盖范围不变（按钮 / 开关 / 输入 / 选项 / 标签页 / 折叠节 / 动态标签 / 事件表 / 统计卡等）。
- 验证：切换过 stats / dict 面板后，全文档 `button/input/select/textarea/.tab/.tagrow/.chip/.sec>h3/a[href]/.lgi/.vtrow/.card/.kv` **0** 个原生 `title`，悬浮头部按钮 / 标签页均弹出 `#tip` 且 `title===''`。

### 4. 导出健壮性
导出前对每个视图**显式调用对应 resize**（`accResize` / `tlResize` / `pvResize`）再重绘，避免「刚切视图即点导出」时画布尚未完成尺寸初始化、导出极小（如 10×10）SVG 的情况。

### 5. 测试矩阵

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| **test_v068**（v0.06.9 回归，重写） | **8** | ✅ SVG 三模式均矢量（含 `<path>/<text>`、不含 `<image>`）+ 交互对象全量无原生 title + 悬浮弹出 `#tip` 气泡 + 无运行时错误 |
| **test_v066**（回归） | **7** | ✅ 整页 PNG 尺寸≈整页、有效区域去空白（stride 等全视图） |
| **合计** | **15** | **全绿** |

---

## v0.06.8 · 2025-08-07（SVG 导出 + 交互对象悬浮提示全覆盖）

> 本轮在 v0.06.7 的 PNG 三模式导出（整页/有效区域/全画布）基础上，补齐 **SVG 三模式导出**，并把**所有交互对象的悬浮提示（title）补全到 100% 覆盖**。

### 1. SVG 导出（PNG 右侧新增"⬇ SVG"按钮）
与 PNG 共用同一个 `pngMode` 范围下拉（全画布 / 有效区域 / 整页），点击「⬇ SVG」按所选范围导出：

| 范围 | 实现 | 产物特征 |
|------|------|----------|
| **整页** | 克隆实时 `#app` DOM → `<foreignObject>` 序列化为独立 `.svg` 文件 | **纯矢量**，浏览器直接打开可无限缩放；canvas 内容转 `toDataURL` 的 `<img>` 嵌入，无 Chromium taint 问题 |
| **整个画布** | 当前激活画布 `toDataURL` 嵌入 `<image>`，`viewBox` 对齐画布像素 | 矢量外壳 + 位图内容，体积小、无四周空白 |
| **有效区域** | 先 `tightCropRect()` 求内容包围盒，再按 `viewBox="x y W H"` 裁剪嵌入 | 等同 PNG 有效区域，但为矢量文件 |

**关键技术点**：SVG 文件（序列化 XML + `foreignObject`）可以**直接保存为文件**用浏览器打开做矢量渲染，天然绕开了「把 foreignObject 画到 `<canvas>` 触发 taint」的限制——这正是 v0.06.7 废弃 foreignObject 方案的原因，但 SVG 作为**文件格式**导出不受影响，因此整页 SVG 得以保留矢量能力。

### 2. 交互对象悬浮提示 100% 覆盖
`bindTooltips()` 采用「curated 字典（按 id 赋值，质量最好）+ 类型/类名兜底 + 数据行懒加载（mouseover 首次悬浮补充）」三级策略，本轮回填了所有遗漏点：
- **curated 字典**补全 `dFile` / `dVals` / `btnGotoX` 等条目；
- **动态渲染面板**（`dictRender` 语义字典、`statsRender` 统计）在 `innerHTML` 构建后立即调用 `bindTooltips()`，确保导入/恢复默认等重渲染路径下新控件也带提示；
- **`.tagrow` 内的复选框**：原先为让外层行承载提示而整框跳过，导致复选框自身无 `title`；改为随父行同步赋予相同提示，复选框与行提示一致；
- `dFile` / `dVals` 在创建处**直接内联 `title`**，不再依赖 `bindTooltips` 的调用时机。

最终验证：切换过 stats / dict 面板后，全文档范围内 `button / input / select / textarea / .tab / .tagrow / .chip / .sec>h3 / a[href] / .lgi` 共 **0** 个元素缺失 title。

### 3. 测试矩阵

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| **test_v068**（新增） | **9** | ✅ SVG 三模式导出（整页含 foreignObject / 全画布&有效区域内嵌 image）+ 交互对象全量 title 覆盖（关键元素 / 标签页 / 折叠节 / 动态标签行 / 全文档 0 缺）+ 无错误 |
| **test_v066**（回归） | **7** | ✅ 整页 PNG 尺寸≈整页、有效区域去空白（stride 等全视图） |
| **合计** | **16** | **全绿** |

---

## v0.06.7 · 2025-08-07（PNG 导出修复：整页真正导出全页面 + 有效区域去空白）

> 本轮修复两项 PNG 导出缺陷：原 foreignObject 方案因 Chromium taint 导致「整页」静默回退为单画布；原 viewPlotRect 对非 atime 视图返回整张画布导致有效区域含大量留白。

### 1. PNG「整页」导出修复（缺陷）
**根因**：`exportStagePng` 通过 `<foreignObject>` 将 DOM 序列化到 SVG 再绘制到离屏画布。Chromium 对包含 `foreignObject` 的 SVG 施加 **taint 安全限制**，使 `off.toBlob()` 抛出 `SecurityError`，被 try/catch 静默捕获后回退到 `exportCanvasOnly()`（仅导出当前激活的单个画布）。因此"整页"模式自始至终从未真正导出过整个前端页面。
**修复**：彻底废弃 foreignObject 方案，改为 **DOM→Canvas 合成器** (`exportPagePng`)：
- 直接遍历实时 `#app` DOM 树，按每个元素的 `getBoundingClientRect` 在离屏画布上重绘背景色、边框、文本。
- 可见的 `<canvas>` 元素通过 `drawImage` 原样并入（保证数据可视化内容完整无损）。
- 跳过 `#loader`/`#tip` 等遮罩元素；对 `<select>` 不递归其 option 子节点。
- 无需外部依赖，无 taint 问题，`toBlob` 正常产出 PNG。

### 2. PNG「有效区域」去空白（缺陷）
**根因**：`viewPlotRect()` 对 `atime` 视图使用硬编码边距（左 66px、下 +20px），对其他视图（hot/stride/reuse/timeline/pipeline）直接返回 `{0,0,W,H}` 整张画布——后者包含大量上下左右留白（如 stride padT=80px、reuse padT=94px 的顶部空白区域）。
**修复**：新增 `tightCropRect(cv)` 函数——对画布逐像素扫描，以四角采样确定背景色基准，求所有非背景像素的最小包围盒（加 3px 余量），自动剔除四周空白。适用于**所有 canvas 视图**，无需逐视图硬编码。

---

## v0.06.6 · 2025-08-07（PNG 整页含完整前端 / 纵轴标签自适应合并）

> 本轮两处更新：PNG「整页」导出范围扩展到整个前端页面；地址-时间视图纵轴刻度在缩小时自动合并/跳过标签，避免文本堆积重叠。

### 1. PNG「整页」导出包含整个前端页面
`exportStagePng` 的抓取目标从 `#stage`（仅主舞台）改为 `#app`（顶栏 + 侧栏 + 主舞台 + 底栏）。「整页」模式现导出**包含顶栏 tabs/按钮、左侧过滤侧栏、主舞台与底栏的完整前端截图**；同时克隆体中移除弹窗（关于/开发报告）以避免遮挡主体。`foreignObject` 跨域 taint 时仍回退到当前画布。

### 2. 纵轴刻度自适应合并（缩小时不重叠）
地址-时间视图的 y 轴刻度改为**按最小像素间距反推步长**：设定标签最小垂直间距 `yPitch=16px`，由 `跨度 × yPitch / 画布高` 反推所需步长。
- **数值地址模式**：步长取 `1/2/5×10ⁿ` 的整齐值（地址为整数，步长至少为 1），并据此统一绘制网格线与标签，标签与网格线对齐不再错位。
- **行模式**：步长随可见行数（跨度）与画布高度自适应放大——画布越矮 / 范围越大，跳过的标签越多，相邻标签恒定保持 ≥16px 间距，支持进一步缩放而不再出现文本堆积重叠。
- 新增 `test_v066.mjs`（11 断言）锁定：整页快照含顶栏/侧栏/主舞台、纵轴相邻标签间距 ≥16px 且大范围下自动跳过标签。

---

## v0.06.5 · 2025-08-07（交互增强：拖拽/滚轮/十字线/PNG 通用化/标签重排）

> 本轮聚焦交互体验与一处导出缺陷修复，覆盖用户提出的 9 项需求。

### 1. 标签重排
访存分析标签左移到时序泳道之后：**时序泳道 → 访存分析 → 流水线事务 → 事件表 → 统计分析 → 语义字典**。

### 2. 时序泳道拖拽修复（缺陷）
`tlMove` 的 pan 分支读取了 `TL.drag.my0`，但 `tlDown` 只设置了 `my` 属性 → `my0` 为 `undefined` → `TL.vScroll=Math.max(0, NaN)=NaN` → 泳道坐标全部变 NaN，图形消失；双击 `tlFit()` 复位后恢复。改为读取 `TL.drag.my`。

### 3. 地址-时间视图双向拖拽
此前 atime 拖拽只能左右移动。现记录鼠标起点的 x/y，水平平移 `t0/t1`、垂直平移 `y0/y1`；纵轴为自动拟合（NaN）态时先在 mousedown 落到数据范围再平移。

### 4. 横向滚动滚轮
时序泳道 / 流水线 / 地址-时间 三视图的 wheel 处理新增分支：当 `|deltaX| > |deltaY|`（触控板左右滑、横向滚轮）时按 `deltaX` 平移时间窗。常规纵向滚轮的缩放、Shift+滚轮的纵轴缩放均不受影响。

### 5. 十字虚线对齐
地址-时间视图选中事件后，在水平、垂直方向各画一条贯穿绘图区的 `C.cross` 色虚线（`[5,4]`），与选中点圆标记配合，便于跨轴读数对齐。

### 6. 同 PC 轨迹受关联高亮控制 + 弱化（缺陷）
此前「同PC轨迹 / 同PC方框」不受左侧「关联高亮」选项框控制。现改为**仅在 `关联高亮=同PC` 时绘制**，与全局关联高亮一致；并弱化连线颜色：聚焦 PC 用 `C.rel` 但 alpha 0.5 / 1.0px（原 0.95 / 1.4px），其余 PC 仅极淡彩色（alpha 0.10）。

### 7. PNG 导出重写（缺陷修复 + 通用化）
- **根因**：`exportPng` 仅处理 `pipeline`/`timeline`，访存分析视图回退到 `TL.cv`（隐藏的时序画布）→ 导出空白图。
- **修复**：按当前标签选择正确画布（`activeCanvas()`：`access→ACC.cv` / `timeline→TL.cv` / `pipeline→PV.cv`）。
- **通用化**：新增 `pngMode` 下拉——`全画布` / `有效区域`（裁剪到带标尺的有效绘图区、去除最外层空白）/ `整页`（主舞台 foreignObject 快照）。
- **兼容**：canvas 视图直接导出画布；表/统计/字典等纯 HTML 视图与「整页」用 foreignObject 把 `#stage` 快照到画布（canvas 元素转成其 `toDataURL` 的 img 保留绘制内容）；若浏览器因 foreignObject 跨域策略污染画布（`toBlob` 抛 SecurityError），自动回退到当前画布导出。
- 导出前会重绘当前视图，避免与 rAF 重排竞态导致空白。

### 8. 按钮悬浮提示
为全部此前缺少 `title` 的按钮补充 tooltip：打开/选择文件/文件夹/粘贴解析/清空、字典导入导出与恢复默认、取值标签保存、详情关闭、事务行高增减、时序全览/放大/缩小、时间窗应用、关联窗口重算、x 态跳转等。

### 9. 回归
| 测试套件 | 断言 | 结果 |
|----------|------|------|
| **test_v065**（新增） | **13** | ✅ 标签顺序/TL拖拽修复/atime双向拖拽/横向滚轮/十字线/relMode门控/PNG三模式非空/无错误 |
| test_pctraj（适配） | 11 | ✅ |
| test_mini | 7 | ✅ |
| test_newfeat | 7 | ✅ |
| test_redun | 13 | ✅ |
| test_filter | 11 | ✅ |
| test_edge | 44 | ✅ |
| test_ux | 22 | ✅ |
| test_xz | 23 | ✅ |
| test_port | 23 | ✅ |
| **合计** | **174** | **全绿** |

---

## v0.06.4 · 2025-08-07（同 PC 轨迹 + 缩略图拖拽选窗）

> 本轮实现用户要求的两个交互增强：地址-时间视图的**同 PC 连线轨迹**与**同 PC 方框标记**，
> 以及**缩略图拖拽选择时间窗口**功能。同时完成进一步对比另一 AI 的功能差异确认。

### 1. 同 PC 轨迹（细线连接连续访问）+ 同 PC 方框

**问题背景：** 此前点击 atime 视图中的事件后，同一 PC 的后续访问之间没有连线，用户无法直观看到该 PC 的执行路径。

**实现方案：**

| 功能 | 控件 | 默认 | 渲染方式 |
|------|------|------|----------|
| 同 PC 轨迹 | `pcTraj` checkbox（工具栏） | ✅ 开启 | 选中事件后：其 PC 用 `C.rel` 色 1.4px 线高亮；其余 PC 淡色 0.8px 线 |
| 同 PC 方框 | `pcBox` checkbox（工具栏） | ❌ 关闭 | 选中事件后：用 6×6 方框框住同 PC 的其它访问 |

**数据层：** `accBuild()` 新增 `byPc: Map<string, Array<{i,addlStr,addl,cyc,tag}>>`，
仅含同时有 ADDL+PC 的可见事件，按 cyc 升序排列。跳过 PC 为空/未定义的事件（NaN 安全）。

**渲染逻辑（`accDrawAtime`）：**
- 全局模式（无选中且点数 < 4000）：所有 PC 均绘制淡色轨迹
- 聚焦模式（有选中）：仅高亮选中事件的 PC 轨迹
- 方框模式：遍历 `byPc.get(selPc)` 中除选中事件外的每个访问，画 6×6 strokeRect

### 2. 缩略图拖拽选窗修复

**问题：** 初始加载时缩略图窗口覆盖全 trace → 所有 canvas 点均落入「内部」→ `miniDown` 选 `pan` 模式 → 拖动无效。

**修复：** `miniMove()` 移除 pan 分支，任意非边缘位置按下并拖动均新建时间选区（橡皮筋），左右边缘仍用于缩放当前选区宽度。行为与另一 AI 的 `bindMini` 一致。

### 3. 进一步对比另一 AI 结论

| 对比项 | 另一 AI (memtrace v1.1.0) | 本工具 v0.06.4 |
|--------|--------------------------|---------------|
| 按 PC 轨迹连线 | `drawMap` `OPT.traj`（per-PC polyline） | ✅ 已实现（`pcTraj`），且支持聚焦/全局双模式 |
| 缩略图拖拽选窗 | `bindMini` 橡皮筋选区 | ✅ 已修复并一致 |
| map 纵轴缩放钳制 | ❌ 未钳制（除零 NaN 风险） | ✅ 已钳制（v0.06 起） |

### 4. 回归测试

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| **test_pctraj**（新增） | **11** | ✅ byPc 分组、像素级渲染差异、无错误 |
| **test_mini**（新增） | **7** | ✅ 新建选区、边缘缩放、无错误 |
| test_redun | 13 | ✅ |
| test_filter | 11 | ✅ |
| 其余 9 套 | ~190 | ✅ |
| **合计** | **~232** | **全绿** |

---

## v0.06.3 · 2025-08-07（对比审查收尾：views.js 复查 + 过滤器锁定）

> 本轮把另一 AI（memtrace v1.1.0）的 `views.js`（808 行）也逐行过了一遍，连同 v0.06.2 已完成的 `core.js`（719 行），
> 构成**完整对照审查**。结论是本工具在六个关键维度均已达标或更优，无需要移植的缺失功能、也无需要修复的共有缺陷。

### 1. views.js 复查结论（六个维度）

| 维度 | 另一 AI | 本工具 |
|------|---------|--------|
| X/Z 未定义态 | `RE_XZ` 误判 `0x1234`，tooltip/drawer 逐字段仍误判 | `isXZ()` 已修正（v0.06） |
| 冗余预取口径 | 无时间窗口，正常预取被大批误判 | 驻留窗口判定（v0.06.2） |
| Math.max 展开 | 文档自提坑却仍提交 `Math.max(...arr)` | 循环 `maxOf/minOf`（v0.06.2） |
| 缩放轴跨度钳制 | map 纵轴缩放未钳制 → 除零 NaN | 横轴/纵轴均钳制 + `\|\|1` 兜底 |
| 字段谓词过滤器 | `&` 串联 + hex 比较 | `= != ~ > < >= <=` + 区间 + PC/ADDL/IID/全文（超集） |
| 地址-时间散点 | `drawMap` 散点 + 按 PC 轨迹 | ACC 视图 `atime` 等价覆盖 |

### 2. 过滤器回归（test_filter.mjs，11 断言）

锁定 `parseExpr`/`hexNum`/`applyFilter` 正确性：真实 16 MB trace 上
`READY=0` 仅留 153 条反压预取、`STAL!=0` 仅留 2019 条 L2 stall、多谓词为 AND（互斥谓词得空集证伪 OR）、
非法数值经 `hexNum` 返回 NaN 被安全跳过、清空恢复全量。

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| regressv05 | 26 | ✅ |
| regressv03 | 23 | ✅ |
| regress_mini | 18 | ✅ |
| regress_opaque | 7 | ✅ |
| test_newfeat | 7 | ✅ |
| test_xz | 23 | ✅ |
| test_port | 23 | ✅ |
| test_edge | 44 | ✅ |
| test_ux | 22 | ✅ |
| test_redun | 13 | ✅ |
| **test_filter**（新增） | **11** | ✅ |
| **合计** | **217** | **全绿** |

---

## v0.06.2 · 2025-08-07（冗余预取判定 + 栈溢出预防）

> 本轮继续对照另一 AI（memtrace v1.1.0）的 `core.js` 分析算法：移植其「冗余预取」统计思路，
> 但修正了其**语义缺陷**；并修掉了一个两处代码共有的 `Math.max(...arr)` 栈溢出隐患。

### 1. 冗余预取（redundant prefetch）判定：移植 + 修正语义缺陷

**另一 AI 的问题：** `core.js` 把"只要该行此前被访问过"就计为冗余（`if(before) redundant++`），**完全无视时间窗口**。
一条 cacheline 在十万周期前被读过，早已被 L2 替换出去——那时重新预取完全正确，原口径却会把这类正常预取大批误判成冗余，
严重高估冗余率、误导预取器调优。

**本工具的正确口径：**

```js
const arr=lsuByAddl.get(a);              // 该行全部 demand 访问（LSU_LD_PIPE3 / LSU_ST_PIPE4），按 cyc 有序
if(arr&&arr.length){
  // 二分找「最后一个 cyc < 预取时刻」的 demand 访问
  let lo=0,hi=arr.length;
  while(lo<hi){const mid=(lo+hi)>>1;if(raw[arr[mid]].cyc<e.cyc)lo=mid+1;else hi=mid;}
  if(lo>0){
    const prev=raw[arr[lo-1]].cyc;
    o.prevUse=e.cyc-prev;
    if(o.prevUse<=W.pfResid)o.redundant=1;   // 仅当仍在缓存驻留窗口内才判冗余
  }
}
```

判据是「上一次 demand 访问距预取时刻 ≤ 缓存驻留窗口 `pfResid`（默认 8192 cyc）」：刚访问过的行大概率还在 L2 里，
此时再预取一次不产生收益，只消耗带宽与 MSHR 表项。驻留窗口按 L2 容量与工作集大小估算，可在侧栏调整。

**UI 集成：**

- 统计页概览区新增「冗余预取」卡片（占比偏高时转橙色警示）
- 预取详情小节：① 距上次访问的分桶直方图；② **驻留窗口敏感性表**（256 / 1024 / 4096 / 8192 / 32768 / 不限），
  当前窗口行高亮标记「◀ 当前」，直观看到换窗口冗余率差多少
- 侧栏「关联窗口」新增「冗余预取判定窗口（缓存驻留）」输入框 → 点「重算事务链路」即时重算

### 2. 预防性修复：Math.max(...arr) 栈溢出

两处代码（本工具 + 另一 AI 的 `core.js`）都用了 `Math.max(...arr)` 把数组铺成实参。数组超过约 **12 万个元素**
就会触发 `RangeError: Maximum call stack size exceeded`。另一 AI 的 `core.js:415` 文档里专门提到了这个坑，
却在 `core.js:339` 的 `Math.max(0,...stall)` 里又提交了同样的写法。

本工具新增循环实现的 `maxOf()`/`minOf()` 助手，替换全部 4 处 spread 调用（延迟直方图、四象限、提前量分桶、
访问复用热度条），从根上消除该风险——直方图/分桶长度取决于输入数据，病态大 trace 上完全可能触发。

### 3. 测试矩阵补充

新增 `test_redun.mjs`（13 断言）：用受控合成 trace（相邻时间戳间隔取 gcd=1，避免解析器把周期单位约分掉）验证——
- 周期邻近的预取判为冗余、间隔远超窗口的不判
- 冗余数**严格小于**「朴素口径（只要访问过就算冗余）」数，即修复实证
- 侧栏调节驻留窗口后重算，冗余数随之变化（大窗口全冗余、小窗口全不冗余）
- 敏感性表正确高亮当前窗口行

### 测试矩阵

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| regressv05 | 26 | ✅ |
| regressv03 | 23 | ✅ |
| regress_mini | 18 | ✅ |
| regress_opaque | 7 | ✅ |
| test_newfeat | 7 | ✅ |
| test_xz | 23 | ✅ |
| test_port | 23 | ✅ |
| test_edge | 44 | ✅ |
| test_ux | 22 | ✅ |
| **test_redun**（新增） | **13** | ✅ |
| **合计** | **206** | **全绿** |

---

## v0.06.1 · 2025-08-07（健壮性 + 交互补齐）

> 本轮以「另一 AI 文档声称的 14 类边界验证」为对照，构造 16 个边界 trace 打本地工具，
> 由此暴露并修复了一个会让**全部快捷键失效**的真实缺陷。

### 1. 修复：快捷键处理器在非 Element 事件源上崩溃（真实缺陷）

**现象：** `keydown` 处理器首行 `if(e.target.matches('input,textarea'))return;`
当键盘事件被直接派发到 `window` / `document`（`e.target` 不是 Element、没有 `matches` 方法）时，
抛出 `TypeError: e.target.matches is not a function`，**整个处理器中断，1-6 / F / Esc / ←→ 全部失效**。

**触发条件：** 焦点不在任何元素上、程序化派发事件、部分辅助技术或浏览器扩展转发的键盘事件。
真实浏览器里 `e.target` 通常是 `document.body`（是 Element），所以平时不发作——属于典型的潜伏缺陷。

**修复：**

```js
const tg=e.target;
if(tg&&typeof tg.matches==='function'&&tg.matches('input,textarea,select,[contenteditable]'))return;
```

顺带把过滤范围从 `input,textarea` 扩展到 `select` 与 `[contenteditable]`。

### 2. 新增：↑/↓ 同缓存行前后导航

- 在当前选中事件所属的**同一 ADDL 访问序列**中前后跳转，用来顺着一条 cacheline 的生命周期走查
- 该事件无 ADDL 时，退化为在全部可见事件中按时间前后移动
- 目标事件不在当前时间窗内时自动居中（复用 `tlFocus`）
- 到达序列首/尾时给出明确提示，不静默无响应

### 3. 改进：空文件 / 低质量输入的用户提示

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 空文件 / 纯空白 | 隐藏加载页，进入空白视图；状态栏"已加载 0 条事件"3.2 秒后消失 | **保持加载页**，显示红色错误块，说明期望格式与注释行规则 |
| 无效行占比 >30% | 末尾附一句"跳过 N 行"，容易被忽略 | 状态栏发出 ⚠ 警告，给出百分比，提示确认格式一致性 |
| 正常文件 | — | 不误报（已验证 CRLF 等正常场景） |

### 4. 新增：「关于」弹窗键盘与鼠标操作速查表

完整列出 1-6 / F / Esc / ←→ / ↑↓ / 滚轮 / Shift+滚轮 / Ctrl+滚轮 / 拖拽 / 双击的行为。

### 5. 新增：16 个边界 trace 健壮性回归

| # | 边界场景 | # | 边界场景 |
|---|----------|---|----------|
| 1 | 空文件 | 9 | 乱码 / 二进制混入 |
| 2 | 纯空白行 | 10 | 超大数值（64 位全 f） |
| 3 | 单行 | 11 | 超大时间戳（接近 2^53） |
| 4 | 全部同时间戳（除零风险） | 12 | x/z 密集 |
| 5 | 时间乱序 | 13 | 缺 PC |
| 6 | 缺 ADDL | 14 | CRLF 换行 |
| 7 | 同行重复 key | 15 | 行尾无分号 |
| 8 | 稀疏字段 | 16 | 单行一万个字段 |

每个场景验证：无异常、六视图均可渲染、缩放不产生退化窗口、统计页无 `NaN`/`undefined`/`Infinity`。
**结果 44/44 通过**——本地解析与渲染链路的健壮性得到实证，不再是"应该没问题"。

### 6. 测试基建改进

`regressv03` 的版本断言由硬编码 `v0.05` 改为**三处版本号互相一致**校验（meta 合法 + title 匹配 + 角标匹配），
以后升版不必再改测试。

### 测试矩阵

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| regressv05 | 26 | ✅ |
| regressv03 | 23 | ✅ |
| regress_mini | 18 | ✅ |
| regress_opaque | 7 | ✅ |
| test_newfeat | 7 | ✅ |
| test_xz | 23 | ✅ |
| test_port | 23 | ✅ |
| **test_edge**（新增） | **44** | ✅ |
| **test_ux**（新增） | **22** | ✅ |
| **合计** | **193** | **全绿** |

---

## v0.06 · 2025-08-07（对比移植 + Bug 修正）

### 1. RTL 仿真未定义态 (x/z) 检测（修正另一 AI 的误判 Bug）

**问题来源：** 对比审查另一 AI（memtrace v1.1.0）的代码时发现其使用 `RE_XZ = /[xXzZ]/` 正则检测未定义态——该写法会把十六进制串 `0x1234` 中的前缀 `x` 误判成未定义态，导致几乎所有含 `0x` 前缀的字段都被标红。

**正确实现：**
- 新增 `isXZ(v)` 函数：先剥离 `0x`/`0X` 前缀与下划线，再判断余下字符是否 **整体由 x/z 组成**（如 `xxxxxxxxxxxxxxxx`）
- 对带 `0x` 前缀的值额外检查：若十六进制位中混有 x/z（如 `0x12xx`），也判定为部分未定义
- 空值、纯数字、正常十六进制、含 X 的符号名（如 MAX）均不误判

**UI 集成：**
- 统计页概览卡片区新增红色 ⚠ 卡片（仅当存在 x/z 时显示）
- 明细面板列出按 `tag.field` 归类的出现次数与占比
- 「跳转到首个 x 态事件」按钮 → 切到时序图 + 居中窗口 + 选中事件
- 详情面板顶部显示告警条；含 x/z 的字段值标红并标注「⚠ 未定义态」
- x 态值不参与十进制转换和位域渲染

### 2. AXI/ACE 协议标准解码（移植自另一 AI）

移植另一 AI 的 AXIDEC 字典并扩展为计算型解码：

| 字段 | 解码方式 | 示例 |
|------|----------|------|
| BURS | 静态映射 | 1 → INCR 地址递增 |
| DOMA | 静态映射 | 1 → Inner-shareable 内层共享 |
| LOCK | 静态映射 | 0 → Normal 普通访问 |
| BARR | 静态映射 | 3 → Synchronisation 同步屏障 |
| SIZE | 幂运算 | 4 → 16 字节/拍 |
| LENG | 结合 SIZE | 0+SIZE=4 → 1 拍突发 · 共 16 字节 |
| CACH | 位域展开 | f → Bufferable · Modifiable · Read-Allocate · Write-Allocate |
| PROT | 三位展开 | 7 → Privileged · Non-secure · Instruction |

### 3. 预取覆盖率 vs 评估窗口曲线（移植自另一 AI）

在统计页预取分析区域新增 SVG 内联曲线图：
- 横轴：评估窗口 W（16~32768 cyc，对数刻度，12 个采样点）
- 纵轴：覆盖率 = 提前量 ≤W 的有用预取 / 全部预取
- 绿色虚线：上界（窗口不限时的最大覆盖率）
- 橙色竖线 + 圆点：当前阈值位置
- 曲线数学自洽性验证通过（单调不减）

### 4. 辅助函数

- 新增 `tlFocus(cyc, span)` — 把时序图居中到指定周期（默认跨度 ≤4000 cyc）

### 测试矩阵

| 测试套件 | 断言数 | 结果 |
|----------|--------|------|
| regressv05 | 26 | ✅ |
| regressv03 | 23 | ✅ |
| regress_mini | 18 | ✅ |
| regress_opaque | 7 | ✅ |
| test_newfeat | 7 | ✅ |
| test_xz | 23 | ✅ |
| test_port | 23 | ✅ |
| **合计** | **127** | **全绿** |

---

## v0.05.2 · 2025-08-06（热修复）

> 修复 v0.04 引入的严重回归：在预览/内嵌的 opaque-origin sandbox 中，**所有按钮无响应**

### 🔍 根因分析（与用户反馈完全吻合）

- 启动序列为 `tlInit();pvInit();accInit();miniInit();bind();` —— `miniInit()` 在 `bind()` **之前**执行。
- v0.04 新增的缩略图模块 `miniInit()` 在第 2175 行有一段**未加保护的** `localStorage.getItem('tpv_mini')` 读取。
- 在结果预览所用的 sandboxed iframe 中（`sandbox="allow-scripts"`，**无** `allow-same-origin`），文档处于 opaque origin，访问 `localStorage` 会**同步抛出 `SecurityError`**：
  `Failed to read the 'localStorage' property from 'Window': The document is sandboxed and lacks the 'allow-same-origin' flag.`
- 该异常从 `miniInit()` 冒泡到启动序列，导致整段 `<script>` 中止，**紧随其后的 `bind()` 永不执行** —— 所有按钮（`btnOpen`/`btnPick`/`btnFolder`/`tlFit`/标签/`relMode` 等）的 `.onclick`/`.onchange` 永远是 `null`，点击全部无响应。
- 这正是「v0.03 正常、v0.04 起按钮失效」的原因：v0.03 没有 minimap（无 `miniInit`），自然不存在未保护的 localStorage 读取。

### 🔧 修复

| ID   | 问题                                                         | 修复                                                                                       |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| F7   | `miniInit()` 读取 `localStorage.tpv_mini` 未加保护，opaque sandbox 下抛错中断启动序列，所有按钮无响应 | 用 `try/catch` 包裹该读取（与主题/字典等其他 localStorage 访问的保护方式一致）；不可用时回退默认位置 `bottom` |

> 注：代码库中其余 6 处 `localStorage` 访问（主题、字典、缩略图位置写出、主题写出）**均已**在 `try/catch` 内，唯独 v0.04 这一处遗漏，故本次仅补这一处即可。

### 🧪 验证

- **pre-fix 复刻对照**：去掉保护后在 opaque sandbox 中复现 —— 所有按钮 `onclick=null(object)`、控制台捕获 `localStorage` SecurityError ✅（坐实回归机制）
- **post-fix**：opaque sandbox 中 `btnOpen/btnPick/btnFolder/tlFit/tabs.onclick=function`、`relMode.onchange=function`、无任何 localStorage/sandbox 报错 ✅
- 新增持久回归 `regress_opaque.mjs`（自起静态服务器 + `sandbox="allow-scripts"` iframe）守护该 bug 不再复现 ✅
- 全量回归：regressv05 26/26、regressv03 23/23、regress_mini 18/18、regress_opaque 7/7，均 0 控制台错误 ✅

---

## v0.05.1 · 2025-08-05（热修复）

> 修复"选择 Trace 文件 / 打开 Trace / 选择文件夹"按钮无响应

### 🔧 修复

| ID   | 问题                                          | 修复                                                                                 |
| ---- | --------------------------------------------- | ------------------------------------------------------------------------------------ |
| F2   | `display:none` 的隐藏文件输入 `.click()` 在部分浏览器/内嵌环境被拦截，文件对话框弹不出，表现为按钮无响应 | 隐藏输入改用 `.hideinput`（opacity+位移隐藏，仍保留在渲染树），`.click()` 跨浏览器可靠触发 |
| F3   | 仅支持单文件选择，没有"选择文件夹"入口         | 新增「选择 Trace 文件夹」按钮 + `#folderPick`（`webkitdirectory`），自动挑选目录内首个 trace 文件 |
| F4   | 头部按钮在某些布局下可能被加载遮罩遮挡         | 表头 `z-index` 由 30 提升到 60，始终位于遮罩（z=50）之上                              |
| F5   | 加载失败静默无提示                             | `loadFile` 增加 try/catch 与 `onerror` 失败提示                                       |
| F6   | 预览/沙箱环境下原生文件对话框被拦截，按钮点不动 | 新增「粘贴 Trace 文本」加载回退：粘贴即解析，不依赖任何文件对话框；加载页显著提示该方式 |

### 🧪 验证

- diag_open.mjs：btnOpen / btnPick / btnFolder 点击均触发 filechooser ✅
- 隐藏输入 `display≠none`、文件夹输入含 `webkitdirectory`+`directory` ✅
- diag_folder.mjs：选择含 trace 与杂文件的目录，正确加载 `1785823039623993868-trace.txt`（110,909 行），loader 隐藏 ✅
- diag_paste.mjs / diag_paste2.mjs：粘贴文本（按钮 / paste 事件两种路径）均成功解析并隐藏 loader ✅
- 0 控制台错误

---

## v0.05 · 2025-08-05

> 关联高亮 + 加载前按钮修复：选中事件后按 PC 或 ADDL 维度连线高亮相关事件

### ✨ 新增

| 模块           | 说明                                                                                                             |
| -------------- | ---------------------------------------------------------------------------------------------------------------- |
| 关联高亮       | 侧边栏「显示选项」新增关联模式下拉（关闭 / 同 PC / 同 ADDL）                                                    |
| 时序泳道关联   | 选中事件后，同维度事件以粉色虚线连接，端点画圆圈，相关点描边高亮                                                |
| 流水线事务关联 | 阶段块级别关联高亮与连线，跨事务/跨行连接                                                                      |
| 访存分析关联   | 地址-时间散点图关联高亮与连线                                                                                  |
| 详情面板摘要   | 新增「关联高亮」摘要区：显示匹配键、可见相关事件数、最多16个快捷跳转按钮                                       |
| `--rel` 色彩   | 三套主题新增 `--rel` CSS 变量（dark: #ff4d9d / light: #d61f7a / sepia: #b5347a）                                |
| 绘制上限       | `REL_MAX=400` 防止极端缩放下重绘卡顿                                                                            |

### 🔧 修复

| ID   | 问题                                          | 修复                                                         |
| ---- | --------------------------------------------- | ------------------------------------------------------------ |
| F1   | 加载前 `#loader` 全屏遮罩拦截舞台区域所有点击 | 点击遮罩任意位置触发文件选择对话框                           |

### 🔧 内部实现

- **`computeRel()`**：独立函数遍历 `S.visIdx` 按 PC 或 ADDL 匹配，结果存入 `S.relSet`（Set\<int\>）+ `S.relCount`
- **TL**：`tlDraw()` 重置 `TL.relPts={}`，密度/非密度分支均捕获 `{x,y}` 坐标；事件循环后绘制虚线 + 端点圆圈
- **PV**：`pvDraw()` 重置 `PV.relPts={}`，阶段块循环捕获中心坐标；行循环后绘制连线
- **ACC atime**：复用 `ACC.points[].i` 索引查找选中/相关点；绘制连线 + 圆圈
- **详情面板**：`selectEvent()` 内提前调用 `computeRel()` 渲染摘要区
- **状态管理**：`S.relMode/relSet/relCount` 三字段驱动；`applyFilter()` / `relMode.onchange` / Escape/dClose 均联动清空

### 🧪 验证

- regressv05.mjs：18/18 通过，0 控制台错误
  - loader 遮罩修复验证（点击触发 filePick）
  - TL 同 PC / 同 ADDL 关联计数正确（8729 / 80）
  - TL/PV/ACC relPts 坐标捕获正确
  - 详情面板显示关联摘要
  - PV 无异常、ACC 点命中正确
  - 关闭模式后 relSet 清空
- regressv03.mjs：23/23 通过（无回归）
- regress_mini.mjs：18/18 通过（无回归）

---

## v0.04 · 2025-08-05

> 时间轴缩略图（minimap）：所有以周期为横坐标的视图统一接入可拖拽/缩放的导航缩略图

### ✨ 新增

| 模块           | 说明                                                                                                             |
| -------------- | ---------------------------------------------------------------------------------------------------------------- |
| 时间轴缩略图   | 头部新增 🗺 按钮，时序泳道 / 流水线事务 / 访存分析-地址时间 三类时间轴视图统一接入缩略图                          |
| 密度直方图     | 缩略图绘制全量可见事件的密度分布（按周期分桶，1000 桶），一眼看清访存密集区                                       |
| 时间窗矩形     | 缩略图上叠加当前可见时间窗，矩形随主视图缩放/平移实时联动                                                         |
| 拖拽平移       | 在矩形内部按下拖拽 = 平移时间窗（保持宽度）                                                                       |
| 边缘缩放筛选   | 拖拽矩形左右边缘 = 改变时间窗起止（即快速缩小/放大分析范围）                                                      |
| 点击重定位     | 在矩形外按下 = 以该点为中心重定位窗口，可继续拖拽                                                                 |
| 双击复位       | 双击缩略图 = 时间窗复位到全范围                                                                                   |
| 位置切换       | 🗺 按钮循环：下方 → 上方 → 关闭；配置写入 `localStorage.tpv_mini`                                                 |
| 导航条整合     | 缩略图启用时，时序泳道画布内自带的底部导航条自动隐藏，由缩略图统一接管导航，避免重复                              |

### 🔧 内部实现

- 新增 `Mini` 模块（`miniInit/miniController/miniMount/miniResize/miniDensity/miniDraw/miniDown/Move/Up/Dbl/miniCycle`）
- 缩略图作为 `.pane` 的 flex 子节点挂载到当前活动时间轴视图（canvaswrap 之上/之下），主画布经 `flex:1` 自动让出 48px 高度
- `tlResize/pvResize/accResize` 读取的仍是 canvaswrap 尺寸，挂载/卸载缩略图后自动重算，无需改动视图内部逻辑
- 密度直方图按 `S.visIdx.length` 缓存，仅在过滤变化时重算，拖拽过程仅重绘小画布
- `miniController()` 为各视图提供统一的 `getWin/setWin` 抽象，TL/PV/ACC 各自接入

### 🧪 验证

- 三视图缩略图均挂载、密度直方图渲染、窗口矩形与主视图联动
- 拖拽平移、边缘缩放、点击重定位、双击复位均生效
- 位置切换 下方/上方/关闭 持久化且刷新后保持
- 缩略图拖拽时主视图不再误触发悬停（已加 `Mini.drag` 守卫）
- 0 控制台错误

---

## v0.03 · 2025-08-05

> 新增「访存分析」标签页：面向所有含 ADDL 的事件，提供四类分析视图

### ✨ 新增

| 模块             | 说明                                                                                     |
| ---------------- | ---------------------------------------------------------------------------------------- |
| 访存分析标签页   | 顶部 Tab 新增「访存分析」，仅纳入含 `ADDL` 的事件                                        |
| 地址-时间视图    | 散点图：横轴=周期、纵轴=ADDL（数值模式）或按缓存行分行（行号模式），缩放/平移/双击复位   |
| 地址热点 Top-N   | 按缓存行访问频次排序，柱状图展示类型构成与周期跨度，点击定位首个事件                     |
| 访问步幅分布     | 基于**全局时间序**的地址增量直方图（跨次访存步长），区分 Δ=0 / Δ>0 / Δ<0                  |
| 重访间隔分布     | 同一缓存行相邻两次访问的周期间隔分桶直方图（[0,3]…[100000,∞)），统计 <10c/<100c/<1000c   |
| 联动             | 四类视图均联动侧栏筛选 / 主题 / 事件高亮；悬停提示、点击定位、滚轮缩放、Shift 拖拽框选   |

### 🔧 内部实现

- 新增 `ACC` 模块（`accInit/accResize/accBuild/accRender/accDrawAtime|Hot|Stride|Reuse`）
- 复用 `S.visIdx`、`S.byAddl`、`palette(css)`、`tagMeta()`、`showTipEvent()`、`selectEvent()`
- **步幅** 与 **重访间隔** 语义分离：前者取全局时间序访问流的地址差，后者取同缓存行相邻访问的周期间隔
- 视口哨兵由 `t0=0,t1=1` 改为 `NaN`，避免初始窗口把散点压成 1 个点
- `accPick` 增加 `ACC.points` 越界保护；hover 高亮增加索引边界判断

### 🧪 验证

- 四视图在真实 trace 下均正常渲染（0 控制台错误）
- 悬停提示正常、点击定位详情面板可打开 ✅
- 缩放/平移/双击复位可用 ✅
- 筛选联动：仅勾选 `LSU_LD_PIPE3` → 19,843 事件 / 1,065 缓存行 ✅
- 步幅修复后展示 18 根柱（此前误算为 1 根）；重访间隔分桶求和 = 总对数 ✅

---

## v0.02 · 2025-08-04

> 增强版：版本号落地、三套主题、同 ADDL 合并、关于弹窗、配套文档

### ✨ 新增

| 模块             | 说明                                                                       |
| ---------------- | -------------------------------------------------------------------------- |
| 版本元数据       | 标题、`<meta>`、logo 角标统一显示 `v0.02` · `konglingjun`                   |
| 三套主题         | 深色（默认）/ 浅色（白）/ 米色（护眼），按钮循环切换                       |
| 主题持久化       | 选择写入 `localStorage.tpv_theme`，刷新自动恢复                            |
| 字体 / UI 同步   | 全部 CSS 变量驱动，字体 / 边框 / 阴影 / 选中底色随主题切换                 |
| 合并选项         | 流水线事务-缓存行生命周期 新增「同 ADDL 合并成一行」                        |
| 合并可见性       | 仅在 `pvKind=line` 时显示，切走自动隐藏并取消选中                         |
| 关于弹窗         | 头部新增 ⓘ 按钮，弹出"开发报告摘要 + 完整更新记录"                       |
| 文档             | 同步产出《开发报告.md》《更新记录.md》两份配套文档置于工作目录             |

### 🎨 主题色彩

| 主题 | 背景   | 主文字 | 强调色 |
| ---- | ------ | ------ | ------ |
| dark | #0e1116 | #dfe6ee | #4ea1ff |
| light | #f6f8fa | #1b2430 | #0b6cd4 |
| sepia | #f2e9d8 | #3a2f21 | #a8611b |

### 🔧 内部实现

- 新增 `palette(css)` 把 CSS 变量读出来供 Canvas 使用，主题切换后画布自动同步
- 早期 inline 脚本在样式生效前读 `localStorage` 写回 `data-theme`，避免首屏主题闪烁
- 合并实现：在 `pvBuild` 内按 ADDL 做合并而非修改底层 `buildTxns()`
  - 不影响 `pvSlow` 长延迟过滤、不影响排序、仍按可见性过滤

### 🧪 验证

- 三套主题 canvas 像素采样：bg 互异、与 CSS 变量一致
- 按钮循环：dark → light → sepia → dark ✅
- 合并：3,567 个分段 → 1,291 个 ADDL 行 ✅
- 切到 load 类型，合并选项自动隐藏并取消勾选 ✅
- 关于弹窗能开/关、内容含版本与作者、能跳到两份 md 文档 ✅
- 0 控制台错误

---

## v0.01 · 2025-08-03

> 首个可用版本：单文件离线 HTML 流水线分析器

### ✨ 新增

- **单文件离线 HTML**：零依赖、可 `file://` 直接打开，无任何 CDN
- **5 个视图**：
  - 时序泳道（按事件类型分行，Canvas，DPR 自适应）
  - 流水线事务（Load / 预取 / 缓存行 / Store 四种聚合）
  - 事件表（通用列 / 全字段）
  - 统计分析
  - 语义字典
- **流水线事务重建**：
  - Load：`IID` 配对 PIPE3→DA→WB，6-bit 环需最近邻匹配
  - 预取：TriPF → L2C_CMP → AXI-AR → 被使用，实现四象限（及时/过早/未用/反压）
  - 缓存行：同 ADDL 按 GAP 切段
  - Store：ST_PIPE4 → AXI AW
- **交互**：
  - 多选事件类型、PC/ADDL/IID/全文搜索、字段表达式
  - 时间窗口锁定
  - 5 个启发式关联窗口可调（侧栏「关联窗口」面板）
  - 单击查看详情、双击复位、滚轮缩放、Shift+拖拽框选
- **导出**：CSV / PNG（当前视图）
- **数据集**：openC910 110,909 条 trace 记录，9 类型，136K 周期跨度

### 🐛 校准复核后修复

| ID  | 问题                                            | 修复                                                          |
| --- | ----------------------------------------------- | ------------------------------------------------------------- |
| B1  | 覆写 Load 缺失 `lat`                            | 强制 `o.lat=o.end-o.start`                                    |
| B2  | 反压预取被误关联到 L2-CMP                       | 预取对象挂 `accepted` 标签，反压者不参与后续链路               |
| B3  | `lsuByAddl` 未排序导致提前量中位数 51437        | `seek()` 在有序数组上做二分                                   |
| B4  | 负 `plotW` 让 typedArray 长度变负 → 崩溃        | 加 `Math.max(20, W-padL-padR)` 保护                           |
| M1  | 91.5% 预取命中过高（无 L1 miss 判定）           | 加 `no-DA + FROM=RB` 判定 L1 miss                             |
| M2  | 预取单一数字笼统                                | 拆四象限                                                      |
| M3  | Store→AW 弱关联被当成因果                      | 同一 AW 被多条 Store 关联时记 `awShare>1` 提示                |

### 🎯 性能

- 解析 110k+ 行 < 0.5s
- 第一次 Canvas 绘制 < 0.15s
- 100k 行事件表近虚拟滚动

### ⚠️ 已知问题

- 启发式关联有"猜"的成分（IID 复用 6-bit 环）
- AW 强一致性合并难判（同一 AW 被多 Store 关联只能给出"弱关联"提示）
- 不支持 trace 增量加载
- 字典语义字段静态（不可编辑）
- Canvas 与打印 PDF 表现略不同

---

## 规划中

| 版本  | 目标                                                                 |
| ----- | -------------------------------------------------------------------- |
| 0.06  | 字段语义字典热编辑 + 详情面板一键复制单事件 K=V 对                    |
| 0.10  | 第二个 trace 文件 diff 视图                                           |
| 0.20  | 时序泳道叠加事件密度直方图                                            |
| 0.30  | 关联算法增强：关联线编号标识、hover 弹出依据窗口、多维度同时高亮       |
| 1.00  | 流水线事务导出为可回放脚本                                            |
