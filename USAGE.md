# 使用说明 · Memory Trace Pipeline Viewer (USAGE)

> 读取 `Time;Tag;K:V;…` 格式的 CPU 访存仿真 trace，以流水泳道呈现
> **LSU → L2C → 预取器 → 总线 → L2 写回** 的完整数据流，并支持**只读**实时跟进仿真器写入。

---

## 1. 快速开始

### 1.1 离线分析（一次性）
直接用浏览器打开 `trace-viewer.html`，点 **📂 载入文件** 或把 trace 拖进窗口。
适合仿真跑完后的复盘。

### 1.2 实时跟进（推荐）
```bash
# 在 trace 文件所在目录启动只读服务
cd /path/to/trace/
python3 /path/to/serve.py          # 默认 8777 端口
```
然后浏览器打开提示的 `http://localhost:8777/trace-viewer.html`。

- 点 **📡 打开并实时跟进**（File System Access，需 https / localhost），周期性增量读取新追加的行；
- 或点 **🌐 URL**，填入 `http://localhost:8777/trace.txt`，前端用 `Range` 请求只拉新增字节。

#### 1.2.1 直接双击打开本页（file://）—— 半自动跟随

若你只是**双击 HTML 文件**以 `file://` 方式打开（而非走 localhost 服务），浏览器会封死所有真·实时通道：
`File System Access` 调用挂起无响应、`fetch()` 被 CORS 拦截、`<input type=file>` 拿到的 `File` 对象在文件被追加后会变不可读。
为此本工具提供**半自动跟随**：

1. 点 **📡 打开并实时跟进**，首次全量载入；
2. 之后每 **4 秒**自动重新弹出文件选择框，**回车重选同一 trace 文件**即增量追加新行（只喂入新增字节）；
3. 顶部出现**跟随横幅**，提供「立即检查」（不等计时直接弹框）/「暂停跟随」/「停止」；标签页隐藏或暂停时不会弹框打扰。

> 仿真重启导致文件变短时，前端会自动整文件重载，不会残留旧数据。
> 想要**完全免手动**的实时跟进，仍建议在 trace 目录运行 `python3 serve.py` 走 localhost（见上）。

URL 直接带参可免操作：
```
http://localhost:8777/trace-viewer.html?url=trace.txt&live=1
# ?url=…        加载并自动开始轮询
# ?url=…&live=0 只加载，不轮询
```

> **全程只读**：只读文件句柄 + 仅 `GET/HEAD` 请求，绝不会写回 trace 文件，对仿真器零影响。
> 若仿真器把文件截断重写，前端会自动检测并从头开始。

---

## 2. 界面布局

```
┌─────────────────────── 顶栏：数据源 / 暂停 / 跟随 / 搜索 / 版本 ───────────────────────┐
├──────────┬───────────────────────────────────────────────┬──────────┤
│  侧栏     │  标签页：流水线 / 地址-时间 / 事务 / 事件表 / 分析  │  详情面板  │
│ 数据源    │                                               │  选中事件  │
│ 事件类型  │              （Canvas / 表格 视图区）            │  cacheline │
│ 过滤器    │                                               │  流水链路  │
│ 选中行    │                                               │            │
│ 视图选项  │                                               │            │
│ 内存性能  │                                               │            │
├──────────┴───────────────────────────────────────────────┴──────────┤
│  状态栏：实时状态 / 事件数 / 时间窗口 / 帧耗时 / 版本            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 五种视图

| 视图 | 内容 | 看点 |
| --- | --- | --- |
| 🔗 **流水线** | 横轴时间、纵轴按 **CORE→L2→PF→BUS** 分组的泳道 | 同 cacheline 相邻事件间画带箭头连线，一次访存如何流到 L2、触发预取、发到总线、再写回，一目了然 |
| 📈 **地址-时间** | 横轴时间、纵轴 cacheline 地址 | 步幅/流式访问呈斜线；预取点（♦）跑在需求点前面多远，反映及时性 |
| 🧾 **事务生命周期** | 按 cacheline 聚合成事务 | 预取提前量、发起→总线、总线→写回等延迟；可排序、可导出 CSV |
| 📋 **事件表** | 全字段虚拟滚动表 | 逐行核对原始字段与译码；跟随最新 |
| 📊 **分析** | 预取/总线/热点/步幅/延迟统计 | 预取准确率、覆盖率、及时性；热点 PC 与 cacheline；每 PC 步幅分布；延迟直方图 |

顶部密度条（概览）是全局缩略图，拖动即可定位时间区间。

---

## 4. 操作速查

| 操作 | 效果 |
| --- | --- |
| 滚轮 | 以光标为锚缩放时间轴 |
| Ctrl+滚轮（地址视图） | 缩放地址轴 |
| Shift+滚轮（流水线） | 纵向滚动泳道 |
| 拖拽 | 平移 |
| Shift+拖拽（流水线） | 框选时间区间并放大 |
| 单击事件 | 选中；高亮其 cacheline 的全部流水事件 |
| 双击 | 全览 |
| `+` `-` `←` `→` | 缩放 / 平移 |
| `F` / `L` | 全览 / 跳到最新 2000 周期 |
| `/` | 聚焦搜索框 |
| `Esc` | 清除选中 |

---

## 5. 搜索语法

顶栏搜索框支持：
- `pc=1e88` — 按 PC 过滤
- `addr=00000ede00` — 按 cacheline 地址
- `iid=05` — 按指令 ID
- `tag=L2C` — 按事件类型（子串）
- 纯文本 — 全字段子串匹配

---

## 6. 字段语义配置（⚙）

`TYPE` / `RESP` / `SRC` 这类编码含义随 RTL 而变。点 **⚙** 用 JSON 定义每个字段的：

- `radix`：进制（16/10/2）
- `bits`：one-hot 位名数组（下标 = 位号）
- `enum`：值 → 含义
- `desc`：字段说明

保存后详情面板与分析页会用你的定义显示。**配置存在浏览器本地**。内置默认值已用本 trace 统计校准，标注见下表。

### 已校准 / 确认的语义（v1.1）

| 字段 | 结论 |
| --- | --- |
| `L2C_CMP.TYPE` | **one-hot**，本 trace 恒为单 bit：位 4=读(0010)、位 6=写(0040)、位 7=写回(0080)、位 11=内部清理/Snoop(0800)、位 12=取指/预取通路(1000)、位 1=特殊(0002)、0=无类型/IFU |
| `L2C_CMP.WRIT` | **已确认**：`WRIT=1 ⟺ TYPE∈{0040,0080}`（写 / 写回） |
| `L2C_CMP.SRC` | **已校准**：`SRC=01` 的 `PC` 恒为 `0000` → 预取 / L2 内部路径；`SRC=00` = 需求或内部发起 |
| `L2C_CMP.RESP` | 5-bit；`bit0=valid`、`bit2=hit`、`bit4=done` 的分布吻合（`00001`=valid，`10001`=valid+done 未命中，`10101`=valid+hit+done 命中） |
| `LSU_LD_WB_pg.FROM` | `RB`=来自 Refill Buffer（miss 回填，与 `BIU_PAD_AR` 强相关）；`DA`=DA 级直接前递 |

> 仍有 `(推测)` 标注的字段（如 `CLCP/STCP/HPCP/MID` 控制位语义）请按你的 RTL 修正。

---

## 7. 性能

- 解析 ~16 MB / 11 万行实测 ~250 ms（单核 Chromium）；
- 流水线视图在 ~3 万可见事件下仍 60 fps；
- 自动检测时钟周期（相邻时间戳差值的众数），可手动覆盖；
- 可设事件保留上限（`侧栏 › 内存/性能 › 保留上限`），自动环形覆盖最早数据，避免长跑占满内存。

---

## 8. 关于 / 更新日志

点顶栏 **ⓘ** 查看当前版本号与完整变更记录（与 `CHANGELOG.md` 同步）。
