# CPU 访存 Trace 流水线分析器 · 开发报告

| 项目       | 内容                                                |
| ---------- | --------------------------------------------------- |
| 版本       | v0.07.1                                            |
| 作者       | konglingjun                                         |
| 形态       | 单文件离线 HTML，零外部依赖，零安装                |
| 入口文件   | `index.html`                                        |
| 配套文档   | `开发报告.md`（本文）、`更新记录.md`               |
| 定位       | openC910 CPU 访存 trace 的前端交互分析器            |

---

## 1. 背景与目标

### 1.1 痛点

openC910 CPU 在 L2 cache / CIU 子系统集成 Triverse 多模式预取器的调试中，会导出上百兆大小的访存 trace 文本，每行为：

```
<TIME>; <TAG>; KEY:VALUE; ...
```

不同事件类型（`LSU_ST_PIPE4`、`LSU_LD_PIPE3`、`LSU_LD_DA_fwd`、`LSU_LD_WB_pg`、`L2C_CMP`、`L2_TriPF`、`TriPF_L2`、`BIU_PAD_AW`、`BIU_PAD_AR` 等）既共享 TIME/PC/ADDL/IID 字段，也含各自独有字段。直接在文本里查定位效率极低：

- 人脑难以对 110k+ 条记录的延迟/因果关系在头脑里建出 pipeline 模型
- 报表统计只能给出聚合数字，掩盖局部异常
- 多核 + 共享 L2 + 预取器 + Store 合并的时序交错关系难以一眼看清

### 1.2 目标

做一个**零依赖**、**单文件**、**可离线**的 HTML 前端，能：

1. 直接打开 trace 文本，无需任何服务端
2. 把每条事件按"泳道"形式在时间轴上展现
3. 跨事件做启发式关联（如 Load 流水线 PIPE3→DA→WB、预取 PF→L2→AR→被使用）
4. 提供交互式筛选、可配置关联窗口、统计汇总、语义字典
5. 关键结论可一键导出（CSV / PNG）

---

## 2. 关键设计

### 2.1 整体架构

```
┌─────────── Header(主题/CSV/PNG/关于) ──────────────┐
│ Sidebar (类型勾选/PC/ADDL/IID/窗口/关联窗口)  │  Stage (Tabs)  │
│                                            │  ┌──────────┐  │
│                                            │  │ 时序泳道  │  │
│                                            │  │ 流水线事务│  │
│                                            │  │ 事件表    │  │
│                                            │  │ 统计分析  │  │
│                                            │  │ 语义字典  │  │
└────────────────────────────────────────────┘  └──────────┘  │
```

**单文件约束**：所有 JS / CSS 都在一个 HTML 内，无 `import`，无 CDN。可以直接 `file://` 打开。代价是 CSS 变量、Canvas 调色板必须靠 `getComputedStyle` 每帧解析。

### 2.2 数据解析

- 解析阶段：每行用 `;` 切分，前两段为 `<TIME>` 与 `<TAG>`，其余以 `KEY:VALUE` 收集
- 周期的归一化：选「时间单位 GCD」作 1 周期。默认 100 000，可改
- 索引：按 `S.byAddl`（缓存行）、`S.byTag`（事件类型）建两套索引

### 2.3 事务（Txn）聚合

四种事务视图，对应**四种不同的链表合并语义**：

| 视图                   | 键       | 关联依据                               |
| ---------------------- | -------- | -------------------------------------- |
| Load 指令流水线        | IID      | PIPE3→DA→WB 三段按 IID 配对            |
| 预取生命周期           | ADDL+时间 | TriPF → L2C_CMP → AR → 被使用        |
| 缓存行生命周期         | ADDL+时间 | 同 ADDL 事件按 GAP 切段               |
| Store 指令             | PC+IID   | ST_PIPE4 → AXI AW                      |

每个事务对象结构：

```js
{ kind, key, addl, pc, start, end, lat, stages:[{name,cyc,idx,tag}] }
```

### 2.4 启发式关联窗口

除 IID 配对外，其余按时间邻近 + ADDL 匹配。阈值在侧栏可调：

| 窗口名             | 默认值 | 意义                              |
| ------------------ | ------ | --------------------------------- |
| 预取→L2-CMP 窗口   | 80     | 预取被 L2 接收的最大延迟          |
| 预取→AXI-AR 窗口   | 4000   | L2 命中失败转 AXI 读的最大延迟    |
| 预取「及时有用」    | 4096   | 提前量上限，超则算"过早"          |
| Store→AW 窗口      | 500    | Store 派发到 AXI AW 的最大延迟     |
| 缓存行分段间隔     | 2000   | 同一缓存行相邻访问的合并上限      |

任何阈值改动都会触发 `buildTxns()` 重算，可重复试验。

### 2.5 渲染层

- **Canvas + DPR 自适应**：所有图形（时间轴、泳道、事务条）都用 Canvas；`window.devicePixelRatio` 适配高分辨率屏
- **CSS 变量驱动主题**：dark/light/sepia 三套变量，画布通过 `palette(css)` 每帧读 → 主题切换无须重启
- **虚拟化**：事件表 100k+ 行用 `display:none` 看不见但参与布局的方式做近虚拟滚动
- **密度切换**：长 trace 自动按时间密度降采样，单事件也能清晰看见

### 2.6 主题系统

CSS 变量集合：

```
--bg --bg2 --bg3 --bg4   层级背景
--line --line2           边框
--fg --fg2 --fg3         文字三级
--acc --acc2             主/辅强调色
--ok --warn --err        状态色
--accSoft --accLine      强调色半透明版（hover/边框）
--rowHi --markHi         选中行底色 / 描边色
--onBar                  画在彩色块上的文字色
--navFill                侧栏小面板底色
--shadow                 阴影
```

`palette(css)` 把这些变量读出来喂给 Canvas 由于 Canvas 不能直接用 CSS 变量。`localStorage` 持久化主题到 `tpv_theme`，避免刷新闪烁。

### 2.7 校准复核（v0.01→v0.02 间）

通过与 Python 基线对账，发现并修复：

| ID  | 问题                                            | 修复                                                          |
| --- | ----------------------------------------------- | ------------------------------------------------------------- |
| B1  | 覆写 Load 缺失 `lat`                            | 强制 `o.lat=o.end-o.start`                                    |
| B2  | 反压预取被误关联到 L2-CMP                       | 预取对象挂 `accepted` 标签，反压者不参与后续链路               |
| B3  | `lsuByAddl` 未排序导致提前量中位数 51437        | `seek()` 在有序数组上做二分                                   |
| B4  | 负 `plotW` 让 typedArray 长度变负 → 崩溃        | 加 `Math.max(20, W-padL-padR)` 保护                           |
| M1  | 91.5% 预取命中过高（无 L1 miss 判定）           | 加 `no-DA + FROM=RB` 判定 L1 miss                             |
| M2  | 预取单一数字笼统                                | 拆"及时 / 过早 / 未用 / 反压"四象限                            |
| M3  | Store→AW 弱关联被当成因果                      | 同一 AW 被多条 Store 关联时记 `awShare>1` 提示                |

对账后，预取"及时有用"率从 91.5% 修正到约 25%（更接近真实），提前量中位数 20~40c，符合 L2 预取经验值。

---

## 3. 功能清单

### 3.1 视图

1. **时序泳道**：按事件类型分行，Canvas 绘制时间轴，支持缩放/平移/框选
2. **流水线事务**：四种聚合视图（Load / 预取 / 缓存行 / Store）
3. **事件表**：虚拟滚动的明细表，支持通用列 / 全字段
4. **统计分析**：按类型、延迟、预取命中率等维度汇总
5. **语义字典**：事件类型与各字段含义说明
6. **访存分析**：面向含 ADDL 事件的四类视图——地址-时间散点、地址热点 Top-N、访问步幅分布、重访间隔分布
7. **同 PC 轨迹与方框**（v0.06.4/0.06.5）：地址-时间视图中选中事件后，用细线连接同一 PC 的连续访问（聚焦高亮其 PC），可选用方框框住其它同 PC 访问；`accBuild()` 新增 `byPc` Map 按 PC 分组并按 cyc 升序排列。**自 v0.06.5 起受左侧「关联高亮=同PC」控制，连线颜色已弱化**
7. **时间轴缩略图**：时序泳道 / 流水线事务 / 访存分析-地址时间 三类时间轴视图统一接入密度直方图 + 可拖拽缩放的时间窗，位置可在主视图上/下方或关闭间切换
8. **关联高亮**：选中事件后按「同 PC」或「同 ADDL（缓存行）」维度高亮并连线相关事件（覆盖 TL/PV/ACC atime），详情面板显示摘要与快捷跳转

### 3.2 交互

- 事件类型多选、PC/ADDL/IID/全文搜索、字段表达式
- 时间窗口锁定
- 5 个启发式关联窗口可调
- 单击查看详情，双击复位
- Shift+拖拽框选
- 滚轮缩放时间轴；**Shift+滚轮缩放纵轴**（泳道高度 / 行高 / 地址范围）；**横向滚轮（|deltaX|>|deltaY|）平移时间窗**（v0.06.5，覆盖时序/流水线/atime 三视图）
- **缩略图空白处拖拽直接框选时间窗口**（v0.06.4 修复：全范围时不再误入 pan 模式，始终橡皮筋选区）
- 地址-时间视图支持**按住上下左右拖拽平移**画面（v0.06.5；此前仅左右）
- 地址-时间视图选中事件后显示**十字虚线**（水平+垂直贯穿绘图区）便于对齐（v0.06.5）
- 标签顺序：时序泳道 → 访存分析 → 流水线事务 → 事件表 → 统计分析 → 语义字典（v0.06.5）
- 导出 CSV / PNG；**PNG 范围下拉：全画布 / 有效区域(带标尺) / 整页**（v0.06.5 修复访存分析导出空白；**v0.06.7 彻底修复：整页用 DOM→Canvas 合成器真正导出整个前端页面（含顶栏/侧栏/底栏），有效区域用像素扫描自动剔除四周空白**）；**v0.06.8 同范围下拉下新增「⬇ SVG」导出，v0.06.9 将其「整个画布 / 有效区域」从位图包裹改为真矢量**：用 `makeSvgCtx()` 录制型 Canvas2D 上下文重跑各视图绘制函数，把 `fillRect/stroke/路径/fillText` 录制成 `<rect>/<path>/<text>` 图元，三种范围均为可无限缩放的真矢量（经 DOMParser 校验 well-formed 且 0 个 `<image>`）；整页为矢量 foreignObject（含把画布替换成的矢量 `<svg>`），浏览器直接打开可无限缩放
- 地址-时间视图纵轴刻度**自适应合并**（v0.06.6）：缩小时按最小像素间距反推步长，数值地址模式取 `1/2/5×10ⁿ` 整齐步长并统一绘网格线+标签，行模式步长随范围/画布高度自适应放大，相邻标签恒定 ≥16px 不重叠，支持进一步缩放
- 三套主题切换 + 持久化
- 关联高亮模式（关闭 / 同 PC / 同 ADDL），选中事件后自动连线高亮相关事件
- **同 PC 轨迹 / 同 PC 方框**切换开关（v0.06.4，访存分析工具栏）
- 文件打开支持「选择 Trace 文件」与「选择 Trace 文件夹」（webkitdirectory，自动挑首个 trace 文件）
- 预览/沙箱环境下文件对话框被拦截时，支持「粘贴 Trace 文本」直接加载（不依赖任何文件对话框）
- 快捷键：`1`–`6` 切视图、`F` 适应全部、`Esc` 关闭详情、`←`/`→` 平移时间轴、**`↑`/`↓` 同缓存行前后导航**
- **交互对象悬浮提示 100% 覆盖**（v0.06.8 起覆盖，v0.06.9 改为自定义气泡消除整窗闪烁）：按钮 / 开关 / 选项 / 输入 / 标签页 / 折叠节 / 动态事件类型标签行 / 统计卡片，悬浮均弹出说明；v0.06.8 用原生 `title` 会触发操作系统原生 tooltip **导致鼠标移上按钮时整个浏览器闪一下**，v0.06.9 改为 `bindTips()`：清空全部原生 `title`，统一用事件委托在 `#app` 上驱动**自定义 `#tip` 气泡**，不再有原生 tooltip 闪烁，全文档 0 个原生 `title`

### 3.3 硬件分析能力

- **Load 延迟**：PIPE3 → WB 配对，P50/P99/Max + 直方图，区分 DA 前递与 RB 回填
- **预取 KPI**：接收率、及时有用率、过早预取、四象限判定、提前量累积分布
- **预取覆盖率 vs 评估窗口曲线**：12 点对数采样 + 上界参考线，避免单一阈值下结论
- **X/Z 未定义态检测**：剥离 `0x` 前缀后判定，统计卡片 + 字段明细 + 一键跳转 + 详情标红
- **AXI/ACE 协议解码**：BURS/DOMA/LOCK/BARR 静态映射，SIZE/LENG/CACH/PROT 计算型解码

---

## 4. 性能要点

- 解析 110k+ 行 < 0.5s（Body 为字符串 + split + 索引）
- 第一次 Canvas 绘制 < 0.15s（不含拖拽）
- 重绘触发：筛选、缩放/平移、主题切换、勾选可见事件、详情选中
- 100k 行事件表用近虚拟滚动（抽屉式 abs-position）

---

## 5. 已知限制

| 限制                                | 说明                                                            |
| ----------------------------------- | --------------------------------------------------------------- |
| 启发式关联有"猜"的成分              | IID 复用（6-bit 环）导致偶发错配，详情面板会给出 ±3000 候选列表 |
| AW 强一致性合并难判                  | 同一 AW 被多 Store 关联只能给出"弱关联"提示                      |
| 不支持 trace 增量加载               | 必须一次性把所有行装载到内存                                      |
| 字典是静态（写死）                  | 字段语义字典未走可编辑（仅 v0.01 留了 KV 编辑入口）              |
| Canvas 与打印 PDF 表现略不同        | 二分位刻度间距靠像素估                       |

---

## 6. 后续路线

已完成（原路线图条目）：

| 版本  | 原定目标                                              | 实际落地 |
| ----- | ----------------------------------------------------- | -------- |
| 0.05  | 字典热编辑                                            | ✅ 语义字典页支持编辑 / 导入导出 JSON / 恢复默认 |
| 0.20  | 时序泳道叠加事件密度直方图                            | ✅ v0.04 时间轴缩略图已含密度直方图，且提前到三视图通用 |

待办：

| 版本  | 目标                                                                 |
| ----- | -------------------------------------------------------------------- |
| 0.07  | 详情面板一键复制单事件 K=V 对；字段名点击跳转到语义字典对应条目       |
| 0.10  | 第二个 trace 文件 diff 视图（对比两次仿真的时序差异）                 |
| 0.30  | 关联算法增强：关联线编号标识、hover 弹出依据窗口、多维度同时高亮       |
| 0.40  | X/Z 态时间分布图：把未定义态按周期聚类，判断是复位期残留还是运行期缺陷 |
| 1.00  | 流水线事务导出为可回放脚本（事务定义 + 窗口阈值 + 过滤规则）         |

---

## 7. 文件清单

```
/workspace/  （Trace_Worker）
├── index.html                   # 主工具（单文件 HTML/CSS/JS）
├── tf.mjs                       # 解析 / 事务计算等脚本模块
├── test_frontend.mjs            # 前端回归测试套件
├── example/                     # 示例 trace
│   ├── samples.json             # 示例清单（相对 example/ 的路径）
│   └── tracesource/trace.txt    # 示例 trace
├── 开发报告.md                  # 本文
├── 更新记录.md                  # 版本变更日志
└── 前端测试报告.md              # 测试产物
```

回归测试套件（沙箱工作区，非交付物）：

| 套件 | 断言 | 覆盖 |
|------|------|------|
| `regressv03` / `regressv05` | 23 / 26 | 各版本核心功能全景 |
| `regress_mini` | 18 | 时间轴缩略图交互 |
| `regress_opaque` | 7 | opaque-origin 沙箱下按钮可用性 |
| `test_newfeat` | 7 | Shift+滚轮纵轴缩放、缩略图拖选 |
| `test_xz` | 23 | X/Z 未定义态检测正确性 |
| `test_port` | 23 | AXI/ACE 解码、覆盖率曲线 |
| `test_edge` | 44 | 16 类边界 trace 健壮性 |
| `test_ux` | 22 | 空文件提示、↑/↓ 导航 |
| `test_redun` | 13 | 冗余预取判定（窗口敏感）+ 侧栏重算 |
| `test_filter` | 11 | 字段谓词过滤器（= != ~ > < >= <=）+ AND 语义 + 非法值安全 |
| **合计** | **217** | |

---

## 8. 故障复盘：v0.04 按钮无响应回归（v0.05.2 修复）

### 8.1 现象
用户反馈：v0.03 一切正常；升级到 v0.04（引入时间轴缩略图 minimap）后，预览/结果展示中「打开 Trace / 选择文件 / 选择文件夹 / 缩放 / 关联模式」等**所有按钮点击均无响应**。

### 8.2 定位
1. 启动序列为 `tlInit();pvInit();accInit();miniInit();bind();` —— `miniInit()` 先于 `bind()` 执行。
2. 全局 `grep` 所有 `localStorage` 访问，发现 v0.04 在 `miniInit()` 内第 2175 行有一处**唯一未加保护**的 `localStorage.getItem('tpv_mini')` 读取；其余 6 处（主题/字典/缩略图位置写出/主题写出）均已在 `try/catch` 内。
3. 结果预览以 sandboxed iframe 渲染（`sandbox="allow-scripts"`，**无** `allow-same-origin`），文档处于 opaque origin，访问 `localStorage` 会同步抛 `SecurityError`。
4. 构造 opaque-origin 沙箱对照实验（去掉保护 vs 保留保护）：
   - **pre-fix**：控制台捕获 `Failed to read the 'localStorage' property ... sandboxed and lacks the 'allow-same-origin' flag`，所有按钮 `.onclick === null`，即 `bind()` 未执行。
   - **post-fix**：无任何 localStorage/sandbox 报错，所有按钮 `.onclick === function`。

### 8.3 结论
未捕获的 `localStorage` 异常在 `miniInit()` 冒泡，中断启动序列，使 `bind()` 永不运行 —— 与「v0.03 无 minimap 故正常、v0.04 起失效」完全吻合。v0.05.1 针对同一症状的多次"无响应"修复（隐藏输入 `.hideinput`、表头 z-index、粘贴回退等）均未触及根因，因为根因在 `miniInit()` 而非 DOM/遮罩层。

### 8.4 修复与防护
- 将第 2175 行读取用 `try/catch` 包裹，不可用时回退默认位置 `bottom`，与既有 localStorage 保护风格一致。
- 新增持久回归 `regress_opaque.mjs`：自起静态服务器并在 `sandbox="allow-scripts"` iframe 中加载页面，断言关键按钮 `onclick` 已绑定且无 localStorage 报错，**防止该 bug 再次静默引入**。

---

## 9. 对比移植：v0.06（修正另一 AI 的 X/Z 误判 + 补齐 AXI/ACE 解码与覆盖率曲线）

### 9.1 背景

用户提供了另一 AI（memtrace-pipeline-explorer v1.1.0）的完整交付物，要求逐行对比审查，按以下规则处理：
- 另一 AI 功能更多 → 移植过来
- 功能类似 → 补充完善本地
- 另一 AI 代码有 Bug → 检查并修复自己的代码

### 9.2 发现的 Bug（已修复）

**另一 AI 的 X/Z 误判（P0）：**
- 其代码 `RE_XZ = /[xXzZ]/` 会把十六进制前缀 `0x1234` 中的 `x` 匹配成未定义态
- 影响范围：几乎所有含 `0x` 前缀的 AXI/地址字段都会被标红
- 我们的修复：`isXZ()` 先剥离 `0x` 前缀，再判断余下字符是否全为 x/z

**另一 AI 的列式存储 dup-key Bug（N/A）：** 其 `putCell()` 覆盖写导致同键不同值时只保留最后一个，我们的逐事件对象模型不受此影响。

### 9.3 移植的功能

| 功能 | 来源 | 本地实现 |
|------|------|----------|
| X/Z 未定义态检测 | 另一 AI 有（但有 Bug） | 重写为正确实现 + 统计卡片 + 详情标红 + 跳转按钮 |
| AXI/ACE 协议解码 | 另一 AI 更完善 | 静态映射（BURS/DOMA/LOCK/BARR）+ 计算型解码（SIZE/LENG/CACH/PROT） |
| 覆盖率 vs 窗口曲线 | 另一 AI 有 | SVG 内联 12 点采样 + 上界线 + 当前阈值标记 |

### 9.4 未移植的功能（评估后决定不移植）

- **步幅分析**：本地 ACC 视图已有完整的访问步幅分布子视图（含直方图、占比、tip），功能等价或更强
- **枚举恢复 orig[]**：本地语义字典编辑器已支持持久化标签映射，且可导出 JSON 团队共享
- **L2C_CMP TYPE/FROM 分布**：本地字段取值分布面板已覆盖所有 tag 的所有枚举字段
- **播放游标**：观赏性大于分析性，且与已有的时间窗拖拽 / 缩略图选窗功能重叠

### 9.5 边界健壮性对照验证（v0.06.1）

另一 AI 的 FEATURES.md 声称「通过 14 个边界 trace 验证」。我们据此构造了 **16 个边界 trace**
（在其 14 类基础上补充「行尾无分号」与「单行一万字段」），逐个灌入本地工具，
每个场景验证：无异常、六视图均可渲染、缩放不产生退化窗口、统计页无 `NaN`/`undefined`/`Infinity`。

**结果 44/44 通过**，但过程中暴露出一个本地真实缺陷：

> `keydown` 处理器直接调用 `e.target.matches()`。当事件派发到 `window`/`document`（非 Element）时
> 抛 `TypeError`，导致 **1-6 / F / Esc / ←→ 全部快捷键失效**。
> 真实浏览器中 `e.target` 通常是 `document.body`（是 Element），因此长期潜伏未被发现。

这正印证了用户要求的第三条——**看别人的代码，是为了回头查自己的代码**。
对照测试的价值不在于抄功能，而在于用别人的质量声明当尺子量自己。

同时发现并修复两个 UX 缺口：

1. 空文件 / 无有效记录时静默隐藏加载页，把用户丢进空白界面
2. 无效行占比过高（如 71%）时提示与正常情况无差别，容易被忽略

### 9.6 补齐的交互

- **↑/↓ 同缓存行前后导航**：另一 AI 的抽屉内有类似能力，本地此前缺失。
  实现上做了增强——无 ADDL 时退化为按可见事件顺序移动，目标不在时间窗内时自动居中，越界给提示。
- **键盘鼠标操作速查表**：写入「关于」弹窗，此前分散在各视图 hint 里。

### 9.7 冗余预取判定 + 栈溢出预防（v0.06.2）

继续对照另一 AI 的 `core.js` 分析算法。这一轮聚焦其「冗余预取」统计与一处共通的隐患。

**移植 + 修正语义缺陷：**

另一 AI 的 `core.js`（约 347–363 行）把"只要该行此前被访问过"就计为冗余（`if(before) redundant++`），**完全无视时间窗口**。
一条 cacheline 在十万周期前被读过，早已被 L2 替换出去——那时重新预取完全正确，原口径却会把这类正常预取大批误判成冗余，
严重高估冗余率。本工具改为**时间窗口判据**：用 `lsuByAddl`（按 cyc 有序）二分查找该缓存行最后一次 demand 访问时刻，
仅当「距预取时刻 ≤ 驻留窗口 `pfResid`（默认 8192 cyc）」才判冗余。

实证对比（新增 `test_redun.mjs`）：用受控合成 trace 注入"间隔 200 cyc 的邻近预取"与"间隔 29563 cyc 的远古预取"，
本地冗余数 = 1，而"朴素口径（只要访问过就算冗余）"数 = 2 —— 即修复可量化验证。

**UI 落地：** 统计页概览区加「冗余预取」卡片；预取详情小节加距上次访问分桶直方图 + **驻留窗口敏感性表**
（256/1024/4096/8192/32768/不限，当前窗口行高亮「◀ 当前」）；侧栏「关联窗口」加「冗余预取判定窗口」输入框，点「重算事务链路」即时重算。

**预防性修复：Math.max(...arr) 栈溢出**

本地与另一 AI 的 `core.js` 都用了 `Math.max(...arr)` 把数组铺成实参。数组超过约 12 万个元素就会触发
`RangeError: Maximum call stack size exceeded`。另一 AI 在 `core.js:415` 的文档里专门提了这个坑，
却在 `core.js:339` 的 `Math.max(0,...stall)` 又提交了同样的写法。本地新增循环实现的 `maxOf()`/`minOf()` 助手，
替换全部 4 处 spread 调用（延迟直方图、四象限、提前量分桶、访问复用热度条），从根上消除该风险。

### 9.8 views.js 逐行复查 + 过滤器锁定（v0.06.3）

完成另一 AI 的 `views.js`（808 行）逐行复查，与 v0.06.2 已完成的 `core.js`（719 行）合起来构成**完整对照**。结论：本工具在六个关键维度均已达标或更优，无需要移植的缺失功能，也无需要修复的共有缺陷。

| 维度 | 另一 AI（core.js / views.js） | 本工具 | 结论 |
|------|------------------------------|--------|------|
| X/Z 未定义态 | `RE_XZ=/[xXzZ]/` 误判 `0x1234`；tooltip/drawer 逐字段仍用该误判 | `isXZ()` 先剥 `0x` 前缀再判（v0.06） | **更优** |
| 冗余预取口径 | "只要访问过就算冗余"，无时间窗口 | 驻留窗口 `pfResid` 判定（v0.06.2） | **更优** |
| Math.max 展开 | 文档自提坑却在 `core.js:339`/`bars()` 仍提交 `Math.max(...arr)` | 循环 `maxOf/minOf` 助手（v0.06.2） | **更优** |
| 缩放轴跨度钳制 | map 视图纵轴缩放**未钳制** → 反复缩放致除零 NaN | timeline 横轴 / ACC 纵轴均钳制 + `\|\|1` 兜底 | **更优** |
| 字段谓词过滤器 | `parseExpr` 仅 `&` 串联 + hex 比较 | `= != ~ > < >= <=` + 周期区间 + PC/ADDL/IID/全文 + 多行，超集 | **更优** |
| 地址-时间散点图 | `drawMap` 散点 + 按 PC 轨迹线 | ACC 视图 `atime` 子视图等价覆盖 | **持平** |

复查还确认双方共有特性（冗余预取统计、覆盖率曲线、L2C_CMP TYPE/FROM 分布、播放游标）本工具均已具备或更强，无需补移植。

**过滤器回归（`test_filter.mjs`，11 断言）：** 以真实 16 MB trace 锁定 `parseExpr`/`hexNum`/`applyFilter` 正确性——
`READY=0` 仅留 153 条反压预取、`STAL!=0` 仅留 2019 条 L2 stall、多谓词为 AND（互斥谓词得空集以证伪 OR）、
非法数值（`DATA>zzzz`）经 `hexNum` 返回 NaN 被安全跳过不崩溃、清空表达式恢复全量 110909 条。

---

## 10. 致谢

- Trace 解析器灵感来自"对半结构化日志的 KV 化处理"模式
- 周期归一化借鉴 GCD 经验法
- 主题色彩参考 Tailwind / Bootstrap 社区常用深浅组合
